<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mostafa.android.rehanadoctors">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher00"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher00"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".splach">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.Login" />
        <activity android:name=".login.Signup" />
        <activity android:name=".calculations.calculationsActivity" />
        <activity android:name=".coupon.couponActivity" />
        <activity android:name=".AboutDevelopersActivity"></activity>
    </application>

</manifest>